version: 1
kind: app
run:
  port: 80
  health:
    path: /auth/login
    contains:
    - 'name="email"'
companions:
  database: mariadb
environment:
  ADMIN_PASSWORD:
    owner: droplive
    format: alphanumeric
    length: 24
    login:
      username: admin@example.invalid
