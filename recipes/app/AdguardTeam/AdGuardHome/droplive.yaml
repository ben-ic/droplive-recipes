version: 1
kind: app
tagline: "Ad-blocking DNS server"
description: "Network-wide ads & trackers blocking DNS server"
repository: https://github.com/AdguardTeam/AdGuardHome
run:
  port: 3000
  health: /login.html
environment:
  ADGUARD_ADMIN_PASSWORD:
    owner: droplive
    format: alphanumeric
    length: 24
    login:
      username: admin
