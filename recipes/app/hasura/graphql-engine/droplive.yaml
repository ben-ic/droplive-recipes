version: 1
kind: app
description: "Generates a realtime GraphQL API over a database, with row-level access control"
repository: https://github.com/hasura/graphql-engine
run:
  port: 8080
  health: /healthz
companions:
  database: postgres
