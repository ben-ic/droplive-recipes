version: 1
kind: app
description: "Edit, convert, split and merge PDF files from the browser"
run:
  port: 8080
  health: /
environment:
  SECURITY_INITIALLOGIN_PASSWORD:
    owner: droplive
    format: password
    login:
      username: admin
