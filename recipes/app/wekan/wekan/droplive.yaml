version: 1
kind: app
description: "The Open Source kanban, built with Meteor"
run:
  port: 8080
  health: /
# Wekan reads MONGO_URL, not DATABASE_URL.
companions:
  database:
    type: mongodb
    bindings:
      MONGO_URL: url
