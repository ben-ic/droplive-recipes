version: 1
kind: app
tagline: "Minimalist feed reader"
description: "Minimalist and opinionated feed reader"
repository: https://github.com/miniflux/v2
run:
  port: 8080
  health:
    path: /readiness
    contains:
    - OK
companions:
  database: postgres
