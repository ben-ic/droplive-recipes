[web]
    # A DropLive demo is already isolated and destroyed after the session.
    # Netdata's Cloud bearer gate would require a separate Netdata account and
    # hides the local metrics that this demo exists to show.
    bearer token protection = no
