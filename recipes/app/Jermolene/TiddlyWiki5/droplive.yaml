version: 1
kind: app
tagline: "Self-contained wiki"
description: "A self-contained JavaScript wiki for the browser, Node.js, AWS Lambda etc"
repository: https://github.com/Jermolene/TiddlyWiki5
# The company this demo is stocked with before the visitor arrives. Its data is
# in the application's own store, so there is no emulator to hang it off.
world: business.saas_company.v1
run:
  port: 8080
  health: /
