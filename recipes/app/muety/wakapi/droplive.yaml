version: 1
kind: app
description: "A minimalist, self-hosted WakaTime-compatible backend for coding statistics"
run:
  port: 3000
  health:
    path: /api/health
    contains:
    - app=1
    - db=1
  data:
  - /data
